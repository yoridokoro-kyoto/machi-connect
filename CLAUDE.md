@AGENTS.md

# まち・コネクト（machi-connect）開発ガイド

このファイルはClaude Codeがこのリポジトリで作業する際の設計方針・ルールをまとめたものです。

## プロジェクト概要

京都市の町内会（neighborhood associations）向けデジタル化SaaSプラットフォーム。
開発・運営元は就労支援A型事業所「こもれび」。複数の町内会を、こもれびがスーパー管理者として一元管理する構成。

**ビジネス背景**
- 京都市は町内会DX推進の予算を持ち、公募型プロポーザルが見込まれるが、こもれびは入札資格を持たないため直接応募はしない
- 区役所・町内会長への直接アプローチと、行政からの紹介ルートを並行して狙う
- 京都市とかつて連携していた競合「いちのいち」の提携は終了済み → 市場機会
- 京都市ICT化推進事業助成金等の補助金活用支援を差別化ポイントにする
- 既存実績：デジタル御朱印・ARお寺鑑賞プロジェクト

## 技術スタック

- フロントエンド: Next.js（App Router）+ TypeScript + Tailwind CSS（PWA）
- バックエンド/DB/Auth/Storage: Supabase（Project ID: `hcjcwckldzunbsfacirz`）、RLS有効
- デプロイ: Vercel（無料枠）、GitHubから自動デプロイ
- 通知: LINE（無料）、Twilio SMS（緊急時のみ、有料）
- パスエイリアス: `@/*` → `./src/*`（tsconfig）
- GitHubリポジトリ: `yoridokoro-kyoto/machi-connect`
- デプロイ先: `machi-connect.vercel.app`

## アーキテクチャ決定事項

- **ロール**: 4階層（`super_admin` / `admin` / `officer` / `member`）
- **機能フラグ**: 組織（町内会）ごとにDBで管理（`organization_features`テーブル）。サイドバーのNAV_ITEMSに新機能を追加するたびに `src/shared/types/index.ts` の `FeatureKey` union型を必ず更新すること
- **ログイン方式**: マジックリンク優先 → 2回目以降はパスキー → パスキー非対応者はGoogleログイン
- **通知**: LINE通知が基本（無料）。SMSは緊急安全確認時のみ（Twilio、有料）
- **世帯管理**: 1世帯1アカウント、世帯主が代表。重複検知は氏名ではなく住所（同姓は多いが同住所は稀なため）。一括登録（CSV）と招待メールの両方に対応

## フォルダ構成

features-based構成に移行済み。

```
src/
  features/
    notices/       お知らせ・回覧板
    households/    世帯管理
  shared/
    types/          FeatureKey等の共通型定義
    lib/            supabaseクライアント等
```

## データベース概要

主要テーブル: `organizations`, `profiles`, `notices`, `circular_confirmations`, `households`, `organization_features`

- `notices`: `image_url`, `image_urls TEXT[]`, `type`, `content`（任意）, `requires_confirmation`, `requires_rsvp`, `rsvp_deadline`
- `circular_confirmations`: `rsvp`列あり
- RLSは全主要テーブルで有効化済み

## 実装済み機能（2026年8月4日時点）

- ログイン/ログアウト（Supabase Auth）
- お知らせ・回覧板の一覧・詳細・投稿・編集・削除（管理者のみ、profilesテーブルのroleで判定）
- 複数画像アップロード（最大5枚、Supabase Storageバケット`notices`）
- PDF印刷（印刷時ボタン非表示は`print.css`で制御）
- 回覧確認ボタン・確認済み/未確認バッジ
- RSVP収集・集計画面（管理者向け、未回答者の氏名表示対応済み）
- タブ対応の戻るナビゲーション（URLパラメータ `?tab=notice` / `?tab=circular`）
- 投稿確認画面
- サイドバーナビ（モバイルはハンバーガーメニュー、機能フラグ対応）
- 世帯管理（追加/編集/削除、招待メール、CSV一括インポート・重複検知・上書き/スキップ選択）

## 未実装・次のアクション（ロードマップ）

- 世帯番号の自動採番（現状は任意入力のまま。連番か組ごとの連番か、実際の町内会の運用ヒアリング後に方式を決定する）
- RSVP期限前リマインダー通知（3日前・1日前）
- LINE通知連携の実装
- イベント・当番管理機能
- 緊急安全確認機能（補助金申請要件の1つ）
- 電子回覧板（既読/未読トラッキング）・アンケート/電子投票・幹部連絡ツール（補助金申請要件の残り3項目）

> 政府補助金申請には「電子回覧板（既読/未読）」「アンケート/電子投票」「緊急安全確認」「幹部連絡ツール」の4機能が要件として必要。

## 開発上の注意点・既知の問題

- **Supabaseのスキーマキャッシュ問題**: `profiles(name)` のようなjoinがキャッシュ起因で失敗することがある。対処法はプロファイルを別クエリで取得し、JavaScript側で`Map`を使ってマージする
- **TypeScriptエラー**: アーキテクチャの純粋さより動作を優先する。`any`型の使用も許容
- **開発環境**: Windows + PowerShell。`&&`は使わず単一コマンドを分けて実行すること。作業ディレクトリの間違い（`C:\Users\user`のまま実行してしまう）に注意し、必ずプロジェクトフォルダ（`Desktop\machi-connect`）に移動してから実行する
- **テストアカウント**: `admin@test.com`（`profiles`テーブルに`role: admin`で手動挿入済み、UUID: `7b5f872d-6517-49bc-a903-4246e5c287b8`）

## コーディング・進め方のルール

- コード変更のたびに、そのまま実行できる`git push`用コマンドを提示すること
- 複雑な変更はチャット内のインラインコードではなく、ダウンロード可能なファイルとして提供すること
- 仕様や設計上の意思決定が必要な場合は、一度に複数質問せず1問1答形式で進めること
- セッションの区切りで開発ログ（Word）を生成する習慣がある

## 行政・営業資料に関する方針（コード外だが一貫させること）

- 行政向け資料と町内会長向け資料は目的が異なるため別文書として作成する（行政向けは連携・政策整合性重視、町内会長向けは現場の利便性・安心感重視）
- 補助金の具体額は資料に明記せず、「費用負担を大幅に軽減できる可能性がある」等ソフトな表現にとどめる
- 高齢者・デジタル不慣れ層への配慮を設計の中心に置く（紙の併用継続、大文字・高コントラストUI、PDF印刷機能）
- Word文書生成（`docx`ライブラリ）では絵文字がレンダリング失敗の原因になるため、`>>`, `■`, `[ ]`等のASCII記号で代替する
